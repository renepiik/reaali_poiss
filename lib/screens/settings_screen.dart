import 'package:flutter/material.dart';
import 'package:reaali_poiss/styles.dart';

class SettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Seaded', style: h4Headline,),
      ),
      body: Container(),
    );
  }
}